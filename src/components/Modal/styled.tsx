import styled from "styled-components";

import Modal from "@mui/material/Modal";

export const StyledContainerModal = styled(Modal)``;

export const StyledTextError = styled.span`
    font-size: 12px;
    color: red;
`;
